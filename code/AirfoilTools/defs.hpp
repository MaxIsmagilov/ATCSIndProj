#pragma once

#include <complex>
#include <vector>

namespace AirfoilTools {

/// @brief complex number type, used throughout the utility
typedef std::complex<double> complex_number;

/// @brief complex vector type, used throughout the utility
typedef std::vector<complex_number> complex_vector;

} // namespace AirfoilTools
